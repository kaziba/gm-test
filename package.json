{
  "name": "gm-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup": "npm i && npm run compile",
    "start": "node build/index",
    "compile": "coffee -o build/ src/",
    "dev": "coffee -o build/ -w src/"
  },
  "author": "eiurur",
  "license": "MIT",
  "dependencies": {
    "gm": "^1.21.1"
  }
}
